export const district = {
    type: "object",
    properties: {
        id_municipality: {type: "number"},
        name: {type: "string"},
        description: { type: "string"},
        state: { type: "boolean"}
    }
}